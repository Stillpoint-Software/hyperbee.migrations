{
  "statements": [
    {
      "statement": "CREATE INDEX sample_seed_src IF NOT EXISTS WITH BODY $idx",
      "bodies": {
        "idx": {
          "settings": { "number_of_shards": 1, "number_of_replicas": 0 },
          "mappings": { "properties": { "id": { "type": "keyword" } } }
        }
      }
    },
    {
      "statement": "CREATE INDEX sample_seed_dst IF NOT EXISTS WITH BODY $idx",
      "bodies": {
        "idx": {
          "settings": { "number_of_shards": 1, "number_of_replicas": 0 },
          "mappings": { "properties": { "id": { "type": "keyword" } } }
        }
      }
    },
    { "statement": "REINDEX UNSAFE(\"destination is a fresh index that will be discarded after this seed run; overwrite-on-retry is intended\") FROM sample_seed_src TO sample_seed_dst" }
  ]
}
