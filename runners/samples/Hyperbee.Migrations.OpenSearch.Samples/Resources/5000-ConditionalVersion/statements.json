{
  "statements": [
    {
      "statement": "WHEN VERSION >= '2.10' CREATE INDEX sample_v210_only IF NOT EXISTS WITH BODY $idx",
      "bodies": {
        "idx": {
          "settings": { "number_of_shards": 1, "number_of_replicas": 0 }
        }
      }
    },
    {
      "statement": "WHEN VERSION < '2.0' DROP INDEX sample_legacy_pre_2x IF EXISTS"
    }
  ]
}
