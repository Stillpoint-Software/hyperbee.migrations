-- Initial schema: secondary indexes for the application's first queries.
-- Authored in the recommended .pql script form (multi-statement, comments,
-- semicolon-terminated). WAIT ensures each index is fully built before the
-- migration proceeds.

CREATE INDEX WAIT idx_users_email  ON test.users (email)  STRING;
CREATE INDEX WAIT idx_users_active ON test.users (active) NUMERIC;
