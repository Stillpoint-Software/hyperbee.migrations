# design/INDEX.md

| #                  | Title                                                                  | Status    | Date       | Summary                                                                                  |
|--------------------|------------------------------------------------------------------------|-----------|------------|------------------------------------------------------------------------------------------|
| opensearch-provider | [OpenSearch Provider — Pragmatic Hybrid Architecture](opensearch-provider.md) | Proposed | 2026-05-02 | Selected hybrid parser+runtime injection; state-machine façade over IBootstrapStep[] pipeline; always-create indices with override; WithProductionDefaults() extension. Recommends ADRs 0011-0014 |
| migration-squashing | [Migration Squashing for Hyperbee.Migrations](migration-squashing.md) | Proposed (destructive-model reframe 2026-05-05) | 2026-05-04 | **Destructive Flyway/Atlas-style codegen-and-replace model.** Operator-initiated squash CLI: spin ephemeral container, apply migrations < N → snapshot A; apply [N..M] → snapshot B; diff A,B; generate squash body that takes A→B. Source files for [N..M] removed at squash creation; ledger rows preserved. Fleet readiness check mandatory before squash. Mature envs auto-mark via `Replaces`; fresh envs run residual + squash body. v1 ships with at least one provider's snapshot-diff strategy (Postgres `PgDumpSnapshotStrategy`). |
| migration-squashing-consensus | [Migration Squashing — Multi-Provider Consensus (Additive)](migration-squashing-consensus.md) | Superseded (additive-model artifact 2026-05-05) | 2026-05-05 | Output of first multi-advocate pressure-test (additive model). Findings on cross-provider universality (CAS abstraction, realtime read obligation, MigrationApplyMode enum, DDL/data verb lists, snapshot fidelity per-provider) were folded into the canonical design's destructive-model reframe. Retained for traceability. |
| migration-squashing-consensus-destructive | [Migration Squashing — Destructive Model Consensus](migration-squashing-consensus-destructive.md) | Ratified + Hardened (Assessment 0007 amendments 2026-05-05) | 2026-05-05 | Output of multi-advocate pressure-test on destructive (codegen + replace) model + Assessment 0007 hardening. 11 original unanimous items (C1–C11) + 2 new universal items (C12 generation determinism + C13 verification container lifecycle + C13a summary artifact). 9 P0 + 10 P1 amendments folded in. v1 sequencing: Postgres only → v1.1 Aerospike+MongoDB → v1.2 Couchbase+OpenSearch. All 8 original open issues resolved. Ready for /nop:plan. |
| migration-squashing-example-aerospike | [Aerospike Squash — Basic Implementation Example](migration-squashing-example-aerospike.md) | Reference (2026-05-05) | 2026-05-05 | Two-artifact emission (structural JSON manifest + replay-captured data-ops C# body). AerospikeTopologySignature, AerospikeDataOpClassifier (Roslyn AST + [DataMigration] attribute + heuristic client.Put detection), AerospikeSquashGenerator (info() snapshot + canonicalize + diff), AerospikeSnapshotCanonicalizer (sort SI/sets, hash UDF bytes), AerospikeSquashVerifier byte-compare. 5-migration sample with full snapshot A/B, diff result, emitted Squash_2000.cs + manifest.json + dataops.cs, honest gaps section. |
| migration-squashing-example-couchbase | [Couchbase Squash — Basic Implementation Example](migration-squashing-couchbase-example.md) | Reference (2026-05-05) | 2026-05-05 | Concrete C# implementation grounding the Couchbase per-provider commitment. CouchbaseTopologySignature (CE/EE + server-major + services), CouchbaseDataOpClassifier (N1QL verb regex + Roslyn KV-call AST), N1qlPredicateCanonicalizer (recursive-descent WHERE-clause parser, hash-sorted commutative children), GsiBuildBarrier (system:all_indexes polling), three-resource emission (statements.json + bucket-settings.json + dataops.cs), companion bootstrap.cs for ledger-bootstrap ranges, CouchbaseSquashVerifier byte-compare. 5-migration sample with full snapshot A/B, diff, emitted artifacts, verification trace, and 5 error-path samples. |
| migration-squashing-example-mongodb | [MongoDB Squash — Basic Implementation Example](migration-squashing-example-mongodb.md) | Reference (2026-05-05) | 2026-05-05 | Concrete C# rejecting EF Core's "replay-only" framing for Mongo. MongoTopologySignature (standalone/RS/sharded + server major), MongoDataOpClassifier (Roslyn over IMongoCollection + aggregation $out/$merge), MongoValidatorCanonicalizer (8-rule JSON Schema canonicalization), volatile-field stripping, structural diff producing createCollection/dropCollection/collMod/createIndex/dropIndex primitives, drop+recreate refusal on immutable changes, MongoSquashVerifier byte-compare. 5-migration sample (createCollection users, capped orders, collMod with $jsonSchema, partialFilterExpression index, $out backfill carried verbatim). |
| migration-squashing-example-opensearch | [OpenSearch Squash — Basic Implementation Example](migration-squashing-example-opensearch.md) | Reference (2026-05-05) | 2026-05-05 | REST-state-diff canonical implementation. OpenSearchTopologySignature (node count + server major + plugins), OpenSearchDataOpClassifier (AST type matching against CompositeStatementAst raw bodies), OpenSearchSnapshotCanonicalizer (per-resource canonicalization with JSON-pointer-keyed array-ordering, painless lexer-based whitespace normalization, _meta discriminator), per-resource set diff producing existing 21-AST primitives, mapping rename via @rename annotation, OpenSearchSquashVerifier with topology-matched container. 5-migration sample (CREATE INDEX, ALIAS SWAP, CREATE POLICY, APPLY POLICY, UPDATE MAPPING) with snapshot A/B, diff fusing UPDATE MAPPING into CreateIndex, emitted statements.json. |
| migration-squashing-example-postgres | [Postgres Squash — Basic Implementation Example (Path-Finder)](migration-squashing-example-postgres.md) | Reference (2026-05-05) | 2026-05-05 | **Path-finder example.** PostgresTopologySignature (server major + extensions[] + collation/locale provider), PostgresDataOpClassifier (regex/lex over SQL keywords + conservative DO $$ classification), PostgresSquashGenerator with full pg_dump --schema-only post-processing pipeline (strip SET preamble, strip dangerous set_config, normalize line endings, extract CREATE EXTENSION to prerequisites.sql, refuse CREATE INDEX CONCURRENTLY), in-process statement classifier (~600 LOC), per-(kind,name) set diff, sequence setval post-emission, PostgresSquashVerifier in-process dump-vs-dump byte-compare. 5-migration sample with raw pg_dump output BEFORE/AFTER post-processing, statement classifier output, emitted Squash_2000.sql + prerequisites.sql + dataops.sql. |
