{
  "statements": [
    {
      "statement": "CREATE INDEX WAIT idx_email ON test.users (email) STRING"
    },
    {
      "statement": "CREATE INDEX WAIT idx_active ON test.users (active) NUMERIC"
    }
  ]
}
